{
  "name": "materialsetu",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/web",
    "packages/shared"
  ],
  "scripts": {
    "dev": "npm run dev -w @materialsetu/web",
    "build": "npm run build -w @materialsetu/web && node scripts/mirror-dist.mjs",
    "typecheck": "npm run typecheck -w @materialsetu/web"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "prettier": "^3.9.6"
  }
}
